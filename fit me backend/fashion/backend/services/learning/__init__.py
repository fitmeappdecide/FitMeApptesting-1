"""Learning Engine (V2 Phase 9). Read-only consumer of analytics_events
(V2 Phase 8). See engine.py::apply_learned_ranking() for the only
function scan_service.py calls, and engine.py::get_insights() for the
admin-facing view."""
